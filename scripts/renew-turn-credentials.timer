# Weekly renewal — credentials are minted with a 30-day TTL, so this leaves
# ample slack even if a run or two is missed.
[Unit]
Description=Weekly renewal of MediaMTX's Cloudflare Realtime TURN credentials

[Timer]
OnCalendar=weekly
Persistent=true

[Install]
WantedBy=timers.target
